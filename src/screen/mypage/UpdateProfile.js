import * as React from 'react';
import { View, Text } from 'react-native';

function UpdateProfile({ navigation }) {
	return (
		<View>
			<Text>프로필 수정 화면이얌</Text>
		</View>
	);
}
export default UpdateProfile;